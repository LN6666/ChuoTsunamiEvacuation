# NewMap P6 Navigation NPC Status

P6 runtime behavior is implemented as a documented local prototype:

- `NewMapNpcCrowdPrototype` creates lightweight humanoid NPCs near the spawn area.
- NPC cap: 8.
- NPCs wander locally.
- Congestion delay is computed for runtime proxy target interaction.
- No road-aware navigation or road-geometry validation is claimed.

Final status: `completed_with_documented_runtime_proxy`.
