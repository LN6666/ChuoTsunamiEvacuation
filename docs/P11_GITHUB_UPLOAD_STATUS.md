# P11 GitHub Upload Status

Status: prepared for final source commit and push.

The final source upload must include source, configs, tests, docs, tools, prompt files, and small JSON reports. It must not include the final EXE, Unity player data folder, release ZIP, raw PLATEAU data, `Library`, `Temp`, `Logs`, or generated build folders.

Build artifacts are outside the repository under:

`D:\UnityProjects\ChuoTsunamiEvacuation-Releases`
