# P10-B Next Steps To P10-C

After user manual playtest, P10-C should handle:

- Windows x64 build
- release package
- README / controls / data sources / known limitations
- attribution and release notes
- high-detail scene/archive backup outside normal Git unless explicitly approved
- build artifact handling

Before building, review P10-B manual playtest issues and apply only quick, low-risk fixes requested by the user.

P10-C should carry forward:

- green frame visibility and performance results
- Player.log warnings/errors
- FPS and stutter observations
- memory and loading time observations
- ResultPanel warning readability status
- light curtain visibility/performance status
- P10-B++ final optimization audit results, including honest no-production-streaming status, AA verification requirements, frame spike counts, GC counts, and disk paging observation steps
